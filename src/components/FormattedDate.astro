---
interface Props {
	date: Date;
}

const { date } = Astro.props
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString('ja-jp', {
      day: 'numeric',
      month: 'short',
      year: 'numeric',
    })
  }
</time>
